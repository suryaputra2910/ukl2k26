/** @type {import('next').NextConfig} */
const nextConfig: import('next').NextConfig = {
  reactStrictMode: true,
  // ✅ JANGAN PAKAI rewrites() disini — Next.js App Router seringkali nge-ignore nya
};

export default nextConfig;